# Cat & Parrot Ancient Karaoke 🎤🐱🦜

Вайб-кодинг приложение, которое генерирует древние караоке-хиты для дуэта кота и попугая с помощью AI. 
Выберите эпоху, жанр, имена персонажей — получите уникальную песню с историческим лором и уровнем дружбы!

## Текущая фаза разработки

**Статус**: Инфраструктура интегрирована → История генераций и лидерборд реализованы → Готовность к рефакторингу UI компонентов

### Завершено (Рефакторинг структуры, улучшение логики и UI)
- ✅ Рефакторинг структуры под Clean Architecture
- ✅ Разделение компонентов и сервисов (KaraokeForm, KaraokeResult)
- ✅ Валидация данных через Zod (формы, API запросы, ответы)
- ✅ Разделение бизнес-логики (services, prompts, validations)
- ✅ Типизация TypeScript (строгие типы для всех модулей)
- ✅ Ленивая инициализация OpenAI клиента
- ✅ Структура `src/` с правильной организацией модулей
- ✅ Исправление шкалы friendship score с -50/+50 на 0-100
- ✅ Улучшение промпта для логического анализа совместимости
  - AI анализирует 4 фактора совместимости (имена, эпоха/жанр, качество дуэта, общее впечатление)
  - Score обосновывается логически с детальным объяснением в поле `reason`
  - Добавлены примеры расчета для высокого/среднего/низкого score
- ✅ Инициализация Shadcn/UI
  - Установлен компонент Button
  - Настроены CSS переменные для монохромной палитры
  - Обновлен Tailwind config для поддержки Shadcn компонентов
- ✅ Создан минималистичный Landing Page
  - Чистый дизайн без градиентов и эмодзи
  - Кнопки "Войти" и "Регистрация" с навигацией на `/login` и `/signup`
  - Монохромная палитра (Black, White, Gray, Slate)
- ✅ Создана страница генерации караоке (`/generate`)
  - Форма ввода данных (имена, эпоха, жанр)
  - Отображение результата генерации
  - Интеграция с API `/api/generate`
- ✅ Создана страница истории генераций (`/dashboard`)
  - Полностью реализована с фильтрацией, сортировкой и пагинацией
- ✅ Создана страница восстановления пароля (`/forgot-password`)
  - Форма ввода email с валидацией через Zod
  - Использование React Hook Form + Shadcn компонентов
  - Минималистичный дизайн
- ✅ Настроена инфраструктура БД и Auth (Этап 1)
  - Установлены зависимости: `@supabase/supabase-js`, `@supabase/ssr`, `@prisma/client`, `prisma`
  - Создана Prisma схема с моделями: User, Generation, LeaderboardEntry
  - Настроен Prisma Client singleton
  - Созданы Supabase клиенты для браузера и сервера (SSR)
- ✅ Создан Middleware для защиты роутов (Этап 2)
  - Проверка авторизации через Supabase Auth с использованием `getUser()` для валидации токена на сервере
  - Защита роутов: `/generate`, `/dashboard`, `/history`, `/profile`, `/leaderboard`
  - Редирект неавторизованных пользователей на `/login`
  - Редирект авторизованных пользователей с `/login` и `/signup` на `/generate`
- ✅ Созданы страницы и API роуты авторизации (Этап 3)
  - Страницы входа и регистрации (`/login`, `/signup`)
  - Страница восстановления пароля (`/forgot-password`) с интеграцией Supabase Auth
  - Страница установки нового пароля (`/reset-password`) с интеграцией Supabase Auth
  - Zod схемы валидации для форм авторизации
  - API роуты: `/api/auth/login`, `/api/auth/signup`, `/api/auth/logout`
  - Интеграция с Supabase Auth (email/password)
  - Автоматическое создание записи пользователя в Prisma при регистрации (с fallback на Supabase триггер)
  - Исправлена проверка сессии в middleware для корректной работы авторизации
  - Автоматический редирект авторизованных пользователей на `/generate` с landing page
  - Создан Header компонент с кнопкой выхода для защищенных страниц
- ✅ Интеграция БД с генерацией (Этап 5 - частично завершен)
  - API route `/api/generate` требует авторизации
  - Генерации автоматически сохраняются в БД через Prisma
  - Каждая генерация связывается с userId
  - Очки дружбы (friendshipScore) сохраняются в таблице generations
  - Обработка ошибок БД без прерывания процесса генерации
  - Исправлен маппинг полей Prisma схемы для соответствия SQL миграции (snake_case → camelCase)
  - Создан скрипт синхронизации пользователей из Supabase Auth в таблицу users
  - Успешно синхронизированы существующие пользователи
  - ✅ Рефакторинг сервисного слоя (Этап 5.2):
    - Добавлена функция `saveGenerationToDatabase()` в `src/services/karaoke.ts`
    - Создан сервис `src/services/generation.ts` для работы с генерациями:
      - `getUserGenerations()` - получение генераций с фильтрацией, сортировкой и пагинацией
      - `getGenerationById()` - получение конкретной генерации по ID
      - `getRecentGenerations()` - получение последних N генераций
      - `getUserGenerationStats()` - статистика генераций пользователя
    - API route `/api/generate` обновлен для использования функции из сервиса
  - ✅ История генераций на странице `/dashboard` (Этап 5.3):
    - API route `GET /api/generations` для получения истории генераций пользователя
    - Компонент `GenerationList` для отображения списка генераций
    - Фильтрация по эпохе, жанру, очкам дружбы
    - Сортировка по дате или очкам дружбы
    - Пагинация для больших списков
  - ✅ Лидерборд на странице `/leaderboard` (Этап 5.4):
    - API route `GET /api/leaderboard` для получения топ пользователей
    - Компонент `Leaderboard` для отображения лидерборда
    - Автоматическое создание записей в лидерборде при генерации
    - Сортировка по очкам дружбы (по убыванию)
    - Пагинация для больших списков
    - Специальные иконки для топ-3 позиций

### Следующий этап (Рефакторинг UI)
- ✅ Создан Header компонент с кнопкой выхода
- ✅ Создан Sidebar компонент с навигацией (Генерация, Мой профиль, Лидерборд)
- ✅ Создана страница лидерборда (`/leaderboard`) - полностью реализована
- ✅ Обновлена страница профиля (`/dashboard`) - полностью реализована с фильтрацией и сортировкой
- ✅ Интеграция БД с сохранением генераций в API `/api/generate`
  - Проверка авторизации пользователя
  - Сохранение генераций в БД через Prisma с привязкой к userId
  - Сохранение очков дружбы (friendshipScore) в таблице generations
  - Автоматическое создание записей в лидерборде при генерации
  - Обработка ошибок БД без прерывания процесса генерации
  - Исправлен маппинг полей Prisma схемы для соответствия SQL миграции
  - Создан скрипт синхронизации пользователей из Supabase Auth в таблицу users
- ✅ Реализация истории генераций на странице `/dashboard` (Этап 5.3)
  - ✅ API route для получения истории генераций пользователя
  - ✅ Компонент отображения списка генераций
  - ✅ Фильтрация и сортировка генераций
- ✅ Реализация лидерборда на странице `/leaderboard` (Этап 5.4)
  - ✅ API route для получения лидерборда
  - ✅ Компонент отображения топ пользователей
  - ✅ Интеграция с таблицей leaderboard_entries
- ⏳ Рефакторинг UI компонентов под минималистичный дизайн (KaraokeForm, KaraokeResult)

## Возможности

- Генерация караоке-песен с учетом эпохи и жанра
- 6 древних эпох на выбор (Каменный век, Древний Египет, Шумеры, Майя, Ацтеки, Средневековье)
- 6 музыкальных жанров (Поп, Рок, Баллада, Регги, Рэп, Опера)
- Исторический лор для каждой песни
- Система дружбы между котом и попугаем (score от 0 до 100)
  - Логический анализ совместимости на основе 4 факторов:
    - Совместимость имен (фонетическая гармония, длина, стиль) — 0-20 очков
    - Сочетаемость эпохи и жанра — 0-20 очков
    - Качество дуэта в песне (синхронность, гармония, распределение ролей) — 0-30 очков
    - Общее впечатление от выступления (атмосфера, целостность, запоминаемость) — 0-20 очков
  - Каждый score обосновывается детальным объяснением в поле `reason`
- Рифмованные тексты с четкой структурой AABB

## Технологический стек

- **Framework**: Next.js 16 (App Router)
- **Language**: TypeScript (строгая типизация)
- **UI**: React 19
- **UI Library**: Shadcn/UI (компоненты на базе Radix UI)
- **Styling**: Tailwind CSS
- **Validation**: Zod (runtime validation)
- **Forms**: React Hook Form + @hookform/resolvers
- **Database**: PostgreSQL (Supabase)
- **ORM**: Prisma
- **Auth**: Supabase Auth
- **AI**: OpenAI API (gpt-4.1)
- **Icons**: Lucide React
- **Utils**: clsx, tailwind-merge, class-variance-authority

## Архитектура

Проект следует принципам Clean Architecture и Separation of Concerns:

```
src/
├── app/                        # Next.js App Router
│   ├── api/                    # API Endpoints
│   │   ├── generate/            # POST /api/generate - генерация караоке
│   │   ├── generations/         # GET /api/generations - история генераций пользователя
│   │   └── leaderboard/         # GET /api/leaderboard - лидерборд пользователей
│   ├── (auth)/                 # Группа роутов авторизации
│   │   ├── login/              # GET /login - страница входа
│   │   ├── signup/             # GET /signup - страница регистрации
│   │   ├── forgot-password/   # GET /forgot-password - восстановление пароля
│   │   └── reset-password/     # GET /reset-password - установка нового пароля
│   ├── (protected)/            # Защищенные роуты
│   │   ├── generate/           # GET /generate - страница генерации караоке
│   │   ├── dashboard/          # GET /dashboard - история генераций пользователя (мой профиль)
│   │   └── leaderboard/        # GET /leaderboard - лидерборд пользователей
│   ├── layout.tsx              # Root Layout
│   ├── page.tsx                # Landing Page (публичная)
│   └── globals.css             # Глобальные стили
├── components/
│   ├── features/               # Бизнес-компоненты (feature-based)
│   │   ├── karaoke/            # Модуль караоке
│   │   │   ├── KaraokeForm.tsx # Форма ввода данных
│   │   │   ├── KaraokeResult.tsx # Отображение результата
│   │   │   └── index.ts        # Экспорты модуля
│   │   ├── generations/        # Модуль истории генераций
│   │   │   └── GenerationList.tsx # Компонент списка генераций с фильтрацией и пагинацией
│   │   └── leaderboard/          # Модуль лидерборда
│   │       └── Leaderboard.tsx   # Компонент отображения лидерборда
│   ├── ui/                     # Shadcn компоненты
│   │   ├── button.tsx          # Компонент Button
│   │   ├── input.tsx           # Компонент Input
│   │   ├── label.tsx           # Компонент Label
│   │   └── card.tsx            # Компонент Card
│   └── shared/                 # Общие компоненты (Header, Sidebar)
│       ├── Header.tsx          # Header с навигацией и кнопкой выхода
│       └── Sidebar.tsx         # Sidebar с навигацией между страницами
├── lib/
│   ├── validations/            # Zod схемы валидации
│   │   ├── karaoke.ts          # Схемы для формы, запроса, ответа
│   │   └── auth.ts             # Схемы для авторизации (forgotPasswordSchema)
│   ├── utils.ts                # Утилиты (cn для классов)
│   ├── supabase/               # Supabase клиенты
│   │   ├── client.ts           # Клиент для браузера
│   │   └── server.ts           # Клиент для сервера (SSR)
│   └── prisma.ts               # Prisma Client singleton
├── services/                   # Бизнес-логика (domain services)
│   ├── karaoke.ts              # Генерация караоке через OpenAI + сохранение в БД
│   ├── generation.ts           # Работа с генерациями в БД (чтение, фильтрация, статистика)
│   └── leaderboard.ts          # Работа с лидербордом (чтение, создание записей, позиции)
├── prompts/                    # Промпты для AI (версионируемые)
│   └── karaoke.ts              # Промпт генерации караоке
└── types/                      # TypeScript интерфейсы
    └── karaoke.ts              # Типы для модуля караоке
```

### Принципы архитектуры

- **Separation of Concerns**: Каждый модуль изолирован и отвечает за свою область
- **Dependency Inversion**: Сервисы не зависят от конкретных реализаций
- **Single Responsibility**: Каждый файл/модуль имеет одну ответственность
- **Type Safety**: Строгая типизация на всех уровнях
- **Validation First**: Все данные валидируются через Zod перед использованием

## Требования

1. Node.js 18+
2. OpenAI API Key
3. Supabase проект (для БД и Auth)
4. PostgreSQL база данных (через Supabase)

## 🚀 Локальный запуск

1. Клонировать репозиторий:
   ```bash
   git clone <repo-url>
   cd ancientkaraoke/cat-parrot-karaoke
   ```

2. Установить зависимости:
   ```bash
   npm install
   ```

3. Создать файл `.env.local` в корне проекта:
   ```bash
   # OpenAI API
   OPENAI_API_KEY=your_api_key_here
   
   # Database (Supabase PostgreSQL)
   # Найти в Supabase Dashboard: Settings > Database > Connection string
   # Используйте Direct Connection (для development) или Connection Pooling (для production)
   DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project.supabase.co:5432/postgres
   
   # Supabase
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
   
   # Supabase Service Role Key (для скрипта синхронизации пользователей)
   # Найти в Supabase Dashboard: Settings > API > service_role key
   SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
   ```
   
   **Важно**: 
   - Замените `[YOUR-PASSWORD]` на пароль вашей базы данных из Supabase Dashboard.
   - Service Role Key нужен только для скрипта синхронизации пользователей (см. `SYNC_USERS.md`).
   
4. Создать таблицы в Supabase:
   - Откройте Supabase Dashboard → SQL Editor
   - Выполните SQL из файла `supabase-migration.sql`
   - Или используйте Prisma: `npx prisma generate && npx prisma db push`
   
   Подробная инструкция в файле `SUPABASE_SETUP.md`

5. Запустить dev-сервер:
   ```bash
   npm run dev
   ```

6. Открыть http://localhost:3000 в браузере

## Переменные окружения

Текущие:
- `OPENAI_API_KEY` - API ключ OpenAI для генерации караоке
- `DATABASE_URL` - URL подключения к PostgreSQL (Supabase)
- `NEXT_PUBLIC_SUPABASE_URL` - URL проекта Supabase
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` - Анонимный ключ Supabase
- `SUPABASE_SERVICE_ROLE_KEY` - Service Role Key Supabase (для скрипта синхронизации пользователей, опционально)

## 🎯 Пример использования

1. Введите имена кота и попугая
2. Выберите древнюю эпоху (например, "Древний Египет")
3. Выберите жанр (например, "Рок")
4. Нажмите "Сгенерировать Хит"
5. Получите результат с текстом песни, историческим лором и уровнем дружбы

## Принципы разработки

- **Clean Architecture**: Разделение на слои (presentation, business logic, data)
- **Separation of Concerns**: Каждый модуль изолирован и независим
- **Type Safety**: Строгая типизация TypeScript на всех уровнях
- **Validation First**: Все данные валидируются через Zod перед использованием
- **Enterprise-grade**: Код готов к масштабированию и поддержке
- **Lazy Initialization**: Ресурсы создаются только при необходимости

## Roadmap

### Фаза 0: Рефакторинг структуры ✅
- Реорганизация кода под Clean Architecture
- Разделение компонентов и сервисов
- Настройка валидации и типизации

### Фаза 1: Улучшение логики генерации ✅
- Исправление шкалы friendship score с -50/+50 на 0-100
- Улучшение промпта для логического анализа совместимости
- Добавление детальных инструкций для AI по расчету очков дружбы
- Обоснование score на основе 4 факторов совместимости

### Фаза 2: UI/UX улучшения ✅
- Инициализация Shadcn/UI
- Создание минималистичного Landing Page
- Интеграция компонентов Shadcn/UI (Button)
- Настройка монохромной палитры

### Фаза 3: Интеграция инфраструктуры ✅
- Supabase Auth для авторизации
- Prisma ORM для работы с БД
- Middleware для защиты роутов
- Сохранение генераций в БД

### Фаза 4: Дополнительные функции ✅
- История генераций (полностью реализована)
- Лидерборд (полностью реализован)
- Профиль пользователя (история генераций на `/dashboard`)

### Фаза 5: Рефакторинг UI (текущая)
- Рефакторинг KaraokeForm и KaraokeResult под минималистичный дизайн
